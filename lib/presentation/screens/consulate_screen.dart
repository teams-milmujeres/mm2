import 'package:flutter/material.dart';

class ConsulateScreen extends StatelessWidget {
  const ConsulateScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ConsulateScreen')),
      body: Center(child: Text('ConsulateScreen')),
    );
  }
}
